import type { SVGProps } from "react";

// Bare plus mark — Solar only ships Add inside a circle/square, so the plain "+" used on
// the create/add buttons is hand-authored (per the icon rule).
export function PlusIcon(props: SVGProps<SVGSVGElement>) {
  return (
    <svg
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      strokeWidth={2.5}
      strokeLinecap="round"
      strokeLinejoin="round"
      aria-hidden="true"
      {...props}
    >
      <path d="M12 5v14M5 12h14" />
    </svg>
  );
}
