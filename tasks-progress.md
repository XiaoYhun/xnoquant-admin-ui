# Tasks progress

Mirrors `user-tasks.md`. Status: ✅ done · 🔄 in progress · ⬜ todo
NOTE: re-read `user-tasks.md` at the START of every task AND before stopping (user edits it live, adds tasks mid-work). Remove items the user removes.

## Tasks
- ✅ **T5** MFT Charts multi-stage overlay — **done + verified in Chrome**. Train→green; Test→green(train)+orange(test) on a continuous axis, both with the **bold xno-builder fill** (line `width: 2`, area gradient full-color top → transparent bottom). Colors copied exactly from xno-builder: train `#38C9A7`, test `#FFA500`, live `#3B82F6` (fallback `#67e1c1`).
  - **Hover/overlap bug fixed** (user report "yellow line becomes green on hover"): this strategy's `/charts` `stages` map is `simulate`(2020→2025, FULL range), `train`(2020→2023), `test`(2023→2025) — `simulate` overlaps train+test. The old generic "draw every stage" logic rendered `simulate` as a full-width green line under the orange Test line, and a missing series-level `color` let hover fall back to the theme palette (green). Rewrote `buildMultiStageOption` to xno-builder's exact model: **only draw train/test/live segments, never `simulate`**; pin series-level `color` + `emphasis`. Verified: Test = clean green+orange, hover keeps orange, tooltip shows only the hovered stage. Removed the temp debug `console.log`.
- ✅ **T6** MFT Performance + Analysis tabs — **done + verified in Chrome**. Wired `PerformanceMftView`/`AnalysisMftView` into `MftResultsView` (Performance/Analysis views); removed placeholder `PerformanceMft`/`AnalysisMft` + orphaned helpers (`twoToneAreaOption`, `splitByZero`, `GREEN`/`RED` consts, `RETURNS_SERIES`/`CORRELATION_ROWS` imports). tsc + eslint clean. HFT results (OverviewView/PerformanceView/RiskView) untouched. **Verified:** Performance = 3 xno-builder stat cards (Transaction Analysis / Performance Metrics / Advanced Metrics) with real API data + green/red + USD; Analysis = IS Testing Status card (All/Pass/Fail/Pending, per-metric target+value+pass check). Event Analytics block is published/submitted-event gated → not exercised on this strategy (code built, tsc clean).
- ✅ **T7** Live stage pill disabled when not eligible — `mft-results-view.tsx` STAGES `TabsTrigger disabled={s.value==='live' && !(strategy?.valid_to_show_live && (strategy?.live_remaining_days ?? 0) <= 0)}`. Verified in Chrome: Live pill greyed/unclickable for the current strategy (not live-eligible).

- 🔄 **T8** hft-platform APIs → HFT features. **Research DONE.** hft-platform = Rust/Axum backend behind `hft-dev.xnoquant.io`, base `/api`, Bearer JWT (Firebase, `AUTH_API_URL/me`), **raw JSON (no envelope)**. Generated types already at `types/api/hft.ts`. Endpoints: `/api/{auth/me, venues[/{id}][/symbols/sync], accounts[/{id}][/dnse/send-otp], strategies[/{id}][/validate,/validate-features,/feature-catalog], symbols, runs[/{id}][/stop,/summary,/symbol-pnl,/equity-curve,/trades,/live,/live/stream]}`. Enums snake_case (VenueType/AccountType/StrategyType/RunMode/RunStatus).
  - **Current state:** Accounts/Venues/Live/Paper/HFT-strategies REAL (partial); **HFT Results (Overview/Performance/Risk) = 100% MOCK — biggest gap.** Detailed inventory + GAPs in `docs/plans/api-integration.md`.
  - **Prioritized plan:** P0 HFT Results wiring (runs summary/symbol-pnl/equity-curve/trades) · P1 run launching (`POST /api/runs`, unblocks paper/live start) · P2 live Stop button (`useStopRun` ready, no onClick) · P3 venues edit (`PUT`) + symbol sync · P4 HFT strategy save/delete/validate · P5 accounts risk/fee + DNSE OTP.
  - **BLOCKERS + decisions (user 2026-07-10):** approach = **"do it all, use judgment"**; **HFT Results = DEFERRED** (backend to compute missing metrics first). So T8 excludes the Overview/Performance/Risk Result views. "Start Bot" not buildable (no start/resume endpoint) → wire Stop only, disable/annotate Start.
  - **Waves:** W1 ✅ · W2 ✅ · W3 ✅ · W4 DNSE-OTP ✅ (risk/fee deferred — design-blocked). T8 substantially complete; only risk/fee UI + HFT save-code/validate remain (both need external input: a Figma / an editable code editor).
    - **W3 ✅ code-verified (tsc clean):** `useDeleteHftStrategy` (`DELETE /api/strategies/{id}`) + `closeEditor` deletes HFT tabs server-side (was local-only, mirrors MFT delete). `useCreateHftStrategy` now takes `strategyType`; create modal shows a Taker/Maker/Arbitrage `Select` for HFT. MFT create/close untouched. Save-code (PUT)/validate/feature-catalog left unwired (HFT code editor is read-only → needs editor work; flagged). NOT browser-verified: delete-on-close now really deletes real strategies → didn't risk it on the flaky renderer; type picker needs the off-screen "+".
    - **W4 ✅ (DNSE OTP, code-verified):** `useSendDnseOtp` (`POST /api/accounts/{id}/dnse/send-otp`) + a "Send OTP" action on DNSE-venue account rows (`account-list.tsx`, transient feedback) + an optional OTP-passcode field in the launch form for DNSE live runs (`simulate-modal.tsx`, threaded into `LaunchRequest.otp_passcode`). `apiPost` now handles 204/no-body. Not browser-verified (0 DNSE accounts on dev). **Still deferred:** risk/fee config UI (`RiskConfig`/`FeeConfig` — no Figma) + HFT save-code/validate (read-only editor).
    - LEFTOVER: `hft-test-verify` HFT strategy still on dev backend — now deletable by closing its tab (W3).
    - **W2 ✅ verified in Chrome:** `simulate-modal.tsx` is now a real launch form (`useLaunchRun` POST `/api/runs`, `useSymbols`). Verified: modal renders all fields; Paper mode "Start paper run" disabled until account+symbol; **Live** mode shows danger warning + required "I understand this places real orders" checkbox gating submit + hides paper-only fields. NOT exercisable: account→symbol cascade + actual launch (0 accounts on dev backend; won't enter API keys / won't fire runs). LEFTOVER: created a test HFT strategy `hft-test-verify` on the dev backend to reach the modal — persists (HFT delete not wired = W3).
    - **W1 ✅** Live-Trading **Stop** button → confirm Dialog → `POST /api/runs/{id}/stop` (`useStopRun` already existed; wired in `live-runs-table.tsx`); **Start** button disabled + tooltip "Start/resume isn't supported by the backend yet" (GAP-7, no endpoint). tsc clean. Verified in Chrome (only a *Stopped* run present → Stop is running-only so not exercisable live; Start shown gated). **Venues edit** → `useUpdateVenue` (`PUT /api/venues/{id}`) + `edit-venue-modal.tsx` + row pencil (`venue-list.tsx`/`page.tsx`); mock parity in `lib/mock/index.ts`. tsc clean. **Verified in Chrome:** Edit-venue modal opens prefilled (Name/Type).
    - **W2 🔄 (user chose "Full launch form"):** wiring the HFT toolbar **Simulate modal** (`simulate-modal.tsx`, was 100% static) into a real `LaunchRequest` → `POST /api/runs`. New hooks `use-symbols.ts` (`useSymbols(venueId)` GET `/api/symbols?venue_id=`) + `useLaunchRun` (in `use-runs.ts`). Field map: account→`account_id`(`useAccounts`), symbols→`symbol_ids[]`, balances→`starting_balances`, execution(max_slice/twap/chase/cancel_ratio/latency), virtual-channel→`live_condition`. Decisions: default **paper**, **live** gated behind a "places real orders" confirm; `data_kind` default `{kind:"tick"}`; `settlement_currency`=first balance currency; `live_condition` null for live. Design's "Rhai cost (ns)" has no schema field → shown but not sent (flag). Data-gap note: run rows carry names only, so launching is a fresh form (not promote-from-paper). Verify: render + request build only, NO live fire.

## New tasks (2026-07-10 batch 3)
- 🟡 **T9** Start Live Trading modal — **redesigned to Figma 13982-130240 + arbitrage logic done** (`start-live-trading-dialog.tsx` full rewrite; `DEMO_ACCOUNTS` removed → `useAccounts`; HFT Execution Type select → Arbitrage shows Account 1+2, else single Account; builds `LaunchRequest` mode=live via `useLaunchRun`). Subagent Chrome-verified layout + arbitrage toggle. tsc clean. **DATA GAP (open):** `PaperRunRow` has no `strategy_id`/`symbol_ids`, so the actual POST stays disabled (`canSubmit` false, visible "not wired for this run yet" note) — to finish, thread the raw `Run` (strategy_id + manifest symbol ids) into the dialog (`run-detail-panel.tsx` currently passes only the display row).
- ✅ **T10** Live Trading detail panel — new `live-run-detail-panel.tsx` mirrors `strategy-detail-panel.tsx` (slide-in from right, width, close); row Action/click opens it (`live-runs-table.tsx` `onOpenDetail`, Stop button `stopPropagation`); `page.tsx` wires `selectedRun`. Subagent Chrome-verified vs `/strategies`. tsc clean.
- ✅ **T11** Editors bar sticky "+" — tabs now in an inner `overflow-x-auto min-w-0` container; "+" is a `shrink-0` sibling outside it (always visible; right-after-last when short, pinned-right on overflow). Verified: "+" right after last tab when list fits; overflow case code-verified.

## Questions
- ✅ **Q1** Does Paper Trading exist in xno-builder or the xalpha API? → **NO to both.**
  - **xno-builder: NO.** Case-insensitive grep for `paper`/`paperTrading`/`paper_trading` across the entire repo = **zero matches**. No paper-trading page, route, component, hook, or endpoint anywhere.
  - **xalpha API: NO.** `types/api/xalpha.ts` (OpenAPI-generated) has **zero** `paper` occurrences — no paper endpoint or schema. The strategy endpoints that DO exist: `/strategies/{id}/stages/{stage}/performance`, `.../evaluations`, `/strategies/{id}/events/{eventId}/correlation`, `.../comparison`, `/stats/performance`.
  - NOTE: this repo's own `app/(dashboard)/paper-trading/` page is unrelated — not backed by any xalpha paper endpoint, and not present in xno-builder.

## Bugs
- ✅ **B8** Editors bar stray vertical scrollbar — the T11 inner `overflow-x-auto` computed `overflow-y:auto` (CSS: one non-visible axis forces the other to auto). Added explicit `overflow-y-hidden` (`editors-bar.tsx`). DOM-verified: `overflowY:"hidden"`, no vertical scroll.
- ✅ **B7** Simulate HFT modal — header + footer fixed, only middle scrolls. `simulate-modal.tsx`: `DialogContent` now `flex flex-col overflow-hidden` (was whole-modal `overflow-y-auto`); header/footer `shrink-0`; body `flex-1 min-h-0 overflow-y-auto`. tsc clean.
- ✅ **B6** Toolbar tag reflects settings — `toolbar.tsx` pill now shows `market` / `universe` (each item hidden when falsy; whole pill hidden if both undefined) instead of hardcoded Crypto/BTC. **Verified in Chrome:** shows `Crypto Spot · BTC` for the active MFT strategy.
- ✅ **B5** All charts hover tooltip number format weird — fixed. (1) Added a `valueFormatter` to the shared `BaseChart` theme tooltip (`base-chart.tsx`) → all charts cap values at 2dp + thousands sep (charts with their own formatter keep it). (2) Multi-stage results tooltip (`mft-results-view.tsx`) also formatted its **title** from raw unix-seconds → date (was showing `1627689600`). tsc clean. Value fix verified in Chrome (`Train: 0` instead of a 12-decimal float); title fix code-complete (re-verify pending).

## Questions / checks
- ✅ **Q3** hft-platform LIVE trading APIs ready? → **PARTIALLY READY.** `mode=live` is NOT stubbed/flag-gated — a live run flows end-to-end and places REAL signed orders for **binance_spot ✅, binance_futures (USDⓈ-M/linear) ✅, dnse ✅**. NOT ready: **tcbs ❌** (stub no-op; live run fails at init `manifest_bridge.rs:140`), **binance inverse/coin-margined futures ❌** (rejected `manifest_bridge.rs:174`).
  - **CRITICAL deploy caveat:** real execution only in the merged `hft-platform` binary; the standalone `api` binary DRAINS/DISCARDS launch/stop commands → run shows `running` but nothing trades. Verify which binary `hft-dev.xnoquant.io` runs.
  - Live monitoring (`/runs/{id}/live`, `/live/stream`) needs `REDIS_URL` (else 404; orders unaffected). Live balances ARE fetched (the run.rs "TODO" comment is stale). No special role for live; DNSE needs OTP + decryptable API keys.
  - Static-review only: signing/parsing paths are implemented + unit-tested, but not attested against live exchanges in prod.
  - **UI implication:** the launch form should restrict LIVE mode to binance_spot / binance_futures(linear) / dnse — TCBS + binance-inverse should be paper-only (not currently guarded).

## Recently completed (user has cleared these from user-tasks.md)
- B4 Aggregate table cell text left-aligned (`mft-results-view.tsx` YearTable) — verified.
- B2 No-autofill Name/Secret; B3 Pencil→separate edit modal (`accounts/`) — done.
- T1 Console, T2 MFT Settings, T3 Live Account redo, T4 HFT accounts API, B1 Settings overflow — user confirmed already done.

## Notes / references
- xno-builder reference: `G:/Develop/xno-builder` — `app/(dashboard)/build/_components/` (`KetQuaStrategy.tsx`, `TongQuanComponent.tsx`); charts in `components/charts/*LineChart.tsx`.
- Our MFT results: `app/(dashboard)/create-strategy/mft-results-view.tsx`; hooks `hooks/api/use-strategy-results.ts`, `use-strategy-run.ts`.
- Verify in Chrome vs xno-builder before marking done. Do NOT edit `user-tasks.md`.
